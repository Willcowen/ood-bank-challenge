class transactions {
    constructor (date, credit, debit) {
        this.date = date,
        this.credit = credit,
        this.debit = debit,
        this.balance = 0
    }
}

module.exports = transactions